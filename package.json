{
  "name": "pulser-backend",
  "version": "1.0.0",
  "description": "Pulser backend node server",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "NODE_OPTIONS='--max-old-space-size=8000' npx tsx watch src/index.ts",
    "build": "tsc",
    "cleanse": "rm -rf package-lock.json && rm -rf node_modules && npm install",
    "serve": "npx tsx watch src/index.ts"
  },
  "author": "Pulser AI",
  "license": "ISC",
  "dependencies": {
    "@trpc/server": "^10.45.2",
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "helmet": "^8.0.0",
    "trpc-openapi": "^1.2.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^5.0.0",
    "typescript": "^5.6.2"
  }
}
